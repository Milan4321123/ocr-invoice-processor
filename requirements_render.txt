# Render-optimized requirements - NO RUST COMPILATION NEEDED
# Force binary wheels only (no compilation)
--only-binary=all
--prefer-binary

# Core FastAPI stack - compatible versions
fastapi==0.85.0
uvicorn==0.18.3
pydantic==1.10.12

# Essential dependencies
python-multipart==0.0.6
python-dotenv==1.0.0
PyJWT==2.8.0
Jinja2==3.1.2
passlib[bcrypt]==1.7.4

# HTTP client
httpx==0.24.1
requests==2.31.0

# Database
psycopg2-binary==2.9.7

# Supabase - using compatible version
supabase==2.0.2

# SendGrid
sendgrid==6.11.0

# Cryptography - version with binary wheels
cryptography==38.0.4

# Email validation
email-validator==2.0.0

# Folder watching
watchdog==3.0.0

# Date utilities
python-dateutil==2.8.2

# Additional stable dependencies
six==1.16.0
typing-extensions==4.7.1

# Rate limiting
slowapi==0.1.9
