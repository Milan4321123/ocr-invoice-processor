# Render-compatible requirements - minimal compilation dependencies
--only-binary=all
--prefer-binary

# Core FastAPI stack - stable versions with Pydantic v1
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==1.10.13

# Essential dependencies
python-multipart==0.0.6
python-dotenv==1.0.0
PyJWT==2.8.0
Jinja2==3.1.2
passlib[bcrypt]==1.7.4

# Supabase - stable version
supabase==2.0.2

# SendGrid
sendgrid==6.11.0

# Test dependencies (remove for production)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
watchdog==3.0.0
