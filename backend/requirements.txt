# Render-compatible requirements - minimal compilation dependencies
--only-binary=all
--prefer-binary

# Core FastAPI stack - compatible with Python 3.11
fastapi==0.95.2
uvicorn[standard]==0.22.0
pydantic[email]==1.10.12
gunicorn==21.2.0

# Essential dependencies
python-multipart==0.0.6
python-dotenv==1.0.0
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
email-validator==2.0.0
pydantic[email]  # Ensure email validation support
cryptography==41.0.7
ecdsa==0.18.0
pyasn1==0.4.8
Jinja2==3.1.2
passlib[bcrypt]==1.7.4

# Supabase - stable version
supabase==2.0.2

# SendGrid
sendgrid==6.11.0

# Test dependencies (remove for production)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.24.1
watchdog==3.0.0
