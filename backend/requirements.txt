fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
supabase==2.0.2
python-dotenv==1.0.0
pytest==7.4.3
pytest-asyncio==0.21.1
httpx>=0.24.0,<0.25.0
pytest-mock==3.12.0

# Additional dependencies for data validation
pydantic==2.3.0

# Folder watching for automatic processing
watchdog==3.0.0

# Email service dependencies for Prüfbericht workflow
# Using SendGrid for professional email delivery
sendgrid==6.11.0
# JWT for secure approval tokens
PyJWT==2.8.0
# Jinja2 for HTML email templating
Jinja2==3.1.2
# Cryptography for additional security features
cryptography==41.0.7

# Authentication dependencies
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Build optimizations for Render deployment
# Specify Python version to avoid compatibility issues
--index-url https://pypi.org/simple/
--extra-index-url https://pypi.org/simple/

# Core FastAPI dependencies