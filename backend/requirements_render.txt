# Render-optimized requirements with compatible versions
# Force binary wheels only (no compilation)
--only-binary=all
--prefer-binary

# Core FastAPI stack - compatible versions
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Essential dependencies
python-multipart==0.0.6
python-dotenv==1.0.0
PyJWT==2.8.0
Jinja2==3.1.2
passlib[bcrypt]==1.7.4

# Supabase - using compatible versions
supabase==2.0.2
httpx==0.24.1
python-dateutil==2.8.2

# SendGrid
sendgrid==6.11.0

# Cryptography - stable version
cryptography==43.0.3

# Email validation
email-validator>=2.0.0

# Folder watching (optional for production)
watchdog==3.0.0

# Remove test dependencies for production deployment
# pytest==7.4.3
# pytest-asyncio==0.21.1
# pytest-mock==3.12.0
