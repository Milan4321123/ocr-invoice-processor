# Render-optimized requirements - NO RUST COMPILATION
# Force binary wheels only (no compilation)
--only-binary=all
--prefer-binary

# Core FastAPI stack - stable versions with guaranteed binary wheels
fastapi==0.100.1
uvicorn==0.23.2
pydantic==2.3.0

# Essential dependencies
python-multipart==0.0.6
python-dotenv==1.0.0
PyJWT==2.8.0
Jinja2==3.1.2
passlib[bcrypt]==1.7.4

# HTTP client and database
httpx==0.24.1
requests==2.31.0
psycopg2-binary==2.9.7

# Supabase - compatible version
supabase==2.0.2

# SendGrid
sendgrid==6.11.0

# Cryptography - compatible version with binary wheels
cryptography==41.0.7

# Email validation
email-validator==2.1.0

# Folder watching
watchdog==3.0.0

# Date utilities
python-dateutil==2.8.2

# Additional stable dependencies
six==1.16.0
typing-extensions==4.8.0

# Rate limiting
slowapi==0.1.9

# JWT handling
python-jose[cryptography]==3.3.0
