# Render-optimized requirements - uses older stable versions
# Force binary wheels only (no compilation)
--only-binary=all
--prefer-binary

# Core FastAPI stack - older stable versions
fastapi==0.68.0
uvicorn==0.15.0
pydantic==1.8.2

# Essential dependencies
python-multipart==0.0.5
python-dotenv==0.19.0
PyJWT==2.4.0
Jinja2==3.0.3
passlib[bcrypt]==1.7.4

# Supabase - using older version for stability
postgrest==0.10.8
httpx==0.24.1
python-dateutil==2.8.2

# SendGrid
sendgrid==6.9.7

# Cryptography - older version with better binary support
cryptography==3.4.8

# Test dependencies (remove for production)
pytest==7.1.3
pytest-asyncio==0.19.0
pytest-mock==3.8.2
watchdog==2.1.9
